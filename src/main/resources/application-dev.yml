spring:
  datasource:
    url: jdbc:postgresql://localhost:5431/WaitingList
    username: noura
    password: administrateur
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true